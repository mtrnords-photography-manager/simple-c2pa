stages:
  - build

.macos_runners:
  tags:
    - proofmode-macos
  before_script:
    - echo "started by ${GITLAB_USER_NAME}"

build_android:
  extends:
    - .macos_runners
  stage: build
  script:
    - /bin/bash ./mobile/android/build.sh
  artifacts:
    paths:
      - ./mobile/android/out/
